# Malcom Implementation

This repo contains the code for Malcom, an efficient implementation of the incremental type system described in the paper "Incremental Bidirectional Typing via Order Maintenance." It contains source code as well as a pre-built application.

The interactive workbench can be run in the browser at the path output by running `make url` in this directory. 
